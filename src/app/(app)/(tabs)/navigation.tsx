import { ComingSoon } from '@/components/ui/ComingSoon';

// TODO: @rnmapbox/maps + Mapbox Directions API — deferred, see product plan.
export default function NavigationScreen() {
  return (
    <ComingSoon
      title="Navigate"
      description="Turn-by-turn navigation for your rides is on the way."
    />
  );
}
